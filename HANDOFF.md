# HANDOFF — battery-diag

> **이 문서는 개정판이 아니라 신규 작성본이다.** w3 TASK [6] 은 기존 HANDOFF.md 의
> §2/§3/§4/§7 개정을 지시했으나, 그 파일은 작업트리·git 이력 전체·디스크 전역·
> 백업 zip 어디에도 없다 (`BLOCKED.md` 참조). 따라서 "기존 반전 3개에 4번째를
> 추가", "열린작업 #6 을 해소로 표시", "docs/06 §5.2 경고" 같은 지시는 원본을
> 봐야만 이행할 수 있어 비워 두었다. 원본을 주면 지시대로 개정한다.
> 아래 내용은 저장소에 실제로 남아 있는 것(코드·결과·커밋 c5df413~w3)만으로 썼다.

최종 갱신 2026-08-20 (w3).

---

## 1. 이 저장소가 푸는 문제

폐배터리 선별센터의 **평균보상 MDP**. 매 기간 배터리가 도착하고, 센터는 각 대에
대해 즉시매각 / 신속검사(EIS) / 정밀검사(충방전) / 보유를 정한다. 정밀검사 용량
CAP 이 희소자원이고, 창고 자리도 유한하다. 목적은 기간당 평균이익 최대화.

- 원자료: 순환자원정보센터 계약결과 공개 (재사용 381건 + 재활용 361건 = 742건).
  `data/pool.csv`, `pass_soh.csv`, `recycle_post.csv` (gitignore — 서버에만).
- 차종은 FLEET 에서 3종(레이·코나·SM3)을 뽑아 쓴다. 확장은 §4 참조.

## 2. 반드시 알아야 할 반전

> 원본 HANDOFF 의 기존 항목 세 개를 확인할 수 없어, 여기서는 **저장소 이력에서
> 확인되는 반전**만 시간순으로 적는다. 번호는 원본과 대응하지 않는다.

**(A) 두 검사는 경합하지 않는다** (2026-08 정정판, `instance.py` 머리말).
신속검사는 EIS 진단기, 정밀검사는 충방전기로 **별도 장비**다. 포항 인라인
자동평가센터 구성이 근거. 따라서 제약은 정밀검사 용량뿐이고,
CAP = min(M_cyc, floor(H_slot·T_p/T_hold)).

**(B) 정확해의 병목은 CSR 빌드지 풀이가 아니다** (v5, 커밋 5d13e21).
GPU SpMV 는 밀리초인데 CSR 빌드가 분~시간이다. 그리고 빌드는 세 번 죽었다 —
fork 교착 → spawn 고정, 워커 부하편차 → 행동수 기준 분할, OOM → 워커가 임시파일로
흘려쓰기(`streambuild.py`). 상태별 분기 수가 중앙 116 / 최대 131,072 로 극단적으로
치우쳐 있어 "행동 수가 같으면 메모리도 같다" 가 성립하지 않는다.

**(C) 갭의 병목은 정확해가 아니라 디코더였다** (v5b~v5c, 29e345d·2042cb1).
사전등록 기준(최적 대비 갭 ≤ 1%)이 큰 칸에서 깨졌는데, 원인은 규모가 아니었다.
`PolicyNet.decode` 가 슬롯 로짓을 루프 **밖에서** 한 번만 계산해, 특징이 같은
슬롯은 반드시 같은 행동을 받았다("같은 유형 3대를 신속 2 + 매각 1 로 쪼개기" 불가).
최적정책의 표현가능 상한이 3/2 칸에서 59.2% 까지 떨어졌고 실측 일치율이 그 천장에
붙어 있었다. 자기회귀 디코더(`PolicyNet(carry=True)`)로 상한이 100% 가 되고
갭이 2.984% → 0.118% 로 떨어졌다. **학습이 느려질 줄 알았는데 오히려 빨라졌다**
(신경망 정책이 최적에 가까워져 정확평가가 빨리 수렴한다).

**(D) 정식화를 NMAX/SMAX 에서 W 로 바꿨고, 기존 g\* 는 아래로 편향이었다**
(w1~w2, 79de2d3·cca8733·4f83763). ← **이번에 추가된 반전**

기존 모형은 미검사 재고를 차종별 상한 NMAX 로, 선별완료 재고를 총 상한 SMAX 로
따로 잘랐다. 두 문제가 있었다.

1. 물리적으로 창고는 하나다. 보관비는 이미 단일 단가로 합산되는데 용량만 따로
   거는 것은 비일관.
2. `n2[k] = min(n2[k]+1, NMAX)` — 야적장이 차면 도착 배터리가 **비용도 수익도 없이
   증발**한다. 현실 대응물이 없다.

새 정식화는 단일 창고 제약 Σn_t + |sc| ≤ W 이고, 넘치는 도착은 **미검사 상태로
즉시 재활용 매각**된다(수익 p_rc×kWh, 매각가치 낮은 차종부터). `Config.W` 를 주면
새 경로, `None` 이면 기존 경로 그대로다.

편향 **방향이 직관과 반대**다. 소실은 수익 0, 강제매각은 수익 +p_rc×kWh 인데
**둘 다 창고 자리를 먹지 않고 다음 상태도 같다**. 전이커널이 동일하고 보상만
다르므로 누락된 것은 비용이 아니라 **수익**이고, 따라서 기존 g\* 는 **아래로**
편향돼 있었다. w1 커밋이 "위로 편향" 이라 적은 것은 오류이며 w2 에서 정정했다.

크기도 컸다. 대응쌍(W=8 ↔ NMAX=2/SMAX=2, 둘 다 유효 총량 8)에서 W 모형이
499,275 높은데, 소실보정이 30% 를 설명하고 **나머지 70% 는 용량 풀링**이다.
기존 2/2 는 "코나만 4대" 를 표현조차 못 한다.

## 3. 논문급 결과

**확정 (유지)**

- **정확해의 한계 실측.** W-정식화에서 |S| = C(W+12,12). 이 하드웨어(62GB RAM,
  RTX 5080 16GB ×2, 스트리밍 빌드 + StreamSolver)에서 **W=8 이 최대**다 —
  |S|=125,970, n_sa=23,652,230, nnz=6.01G, CSR 72.1GB, 빌드 1,002초, 해 644초.
  W=9 는 CSR 추정 1,712GB(표본 과대 1.47배 보정해도 ~1,160GB)로 NVMe 여유
  631GB 를 넘어 디스크에서 막힌다.
- **창고 한 자리의 그림자가격** Δg\*/ΔW = 91,691 → 58,989 → 35,891 → 22,770
  (W=4→8). 오목. 임대료와 직접 견줄 수 있는 값.
- **최적정책 구조**: 레이(R1)는 전 구간 100% 무검사 즉시매각, SM3 도 대부분
  즉시매각, 검사는 코나 집중. 양끝 생략 구조가 유지된다. 다만 W 가 커지면
  정밀직행이 줄고(0.1439→0.0300) 선별보유가 는다(0.6004→2.5678).
- **자기회귀 디코더**: W 모형 W=4~6 에서 carry 갭 0.002~0.009%, legacy 는
  1.7~17.8%(W=6 시드1 은 48.6% 로 학습 실패). 기존 정식화에서도 5/5 칸이
  사전등록 기준 ≤1% 를 통과한다.
- **표현력 감사 방법론**: 최적정책 라벨을 제약그래프의 순환검출로 검사해
  디코더의 표현가능 상한을 낸다. 독립 구현 두 개가 같은 값을 냈다.
- **SMAX 체감수확**: 2→3 이 107,993, 3→4 가 58,369 (체감비 0.540).
  소실보정에 전혀 움직이지 않는다.

**철회**

- ~~"NMAX 효과가 SMAX 효과의 2.2배"~~ — 소실보정 후 1.25배. 증분의 43.6% 가 인공물.
- ~~"가법분해가 성립한다 (잔차 0.081%)"~~ — 보정 후 0.482%, 부호 반전.
- ~~g\* 의 절대 수준을 인용한 진술~~ — 5칸 전부 0.49~3.76% 아래로 편향.
- ~~w1 의 "무료 소실이 g\* 를 위로 편향시킨다"~~ — 방향이 반대다.
- ~~"소실을 보정해도 최적정책은 그대로"~~ — w3 [1] 재최적화에서 상태의 6~7% 가
  행동을 바꾼다.

**주의해서 쓸 것**

- 벤치마크 갭은 소실 오염에 **완전히 무관하지 않다**. 정책마다 소실가치가 달라
  갭이 최대 ±0.5%p 움직인다(NMAX=2 칸은 감소, NMAX=3 칸은 증가). 다만 최적에
  가까운 정책일수록 이동이 작다 — 3/2 에서 legacy DCL +0.127%p, carry DCL +0.009%p.
  결론을 뒤집을 크기는 아니지만 "무관" 이라고 쓰면 안 된다.

## 4. 열린 작업

> 원본 HANDOFF 의 번호 체계를 확인할 수 없어 새로 매긴다.

| # | 항목 | 상태 |
|---|---|---|
| 1 | W≥9 정확해 | **불가**(디스크). 근사만 가능 |
| 2 | W=7·8 의 DCL(carry) | w3 [3] 진행 |
| 3 | 차종 사다리 확장 \|T\|=4·5 | w3 [4] 진행 |
| 4 | φ 민감도 스윕 | w3 [5] 진행 |
| 5 | W 모형에서의 표현가능 상한 재측정 | 미착수 |
| 6 | 원자료 접근 | **해소**. `data/*.csv` 실측본이 서버에 있고 `load_cached` 로 읽힌다 |
| 7 | 선별축 우선 디코딩 | 미착수. carry 의 미검사축 잔여용량이 "선별품 전량 보유" 가정의 보수적 근사인데, 선별축을 먼저 디코딩하면 근사를 없앨 수 있다 |

## 5. 코드 지도

```
src/battery_diag/
  instance.py    MDP 정의. Config.W 로 정식화 전환, credit_loss 로 소실보정 MDP
  build.py       in-RAM CSR 빌드 + 디스크 캐시
  streambuild.py 스트리밍 빌드 (CSR 이 RAM 을 넘을 때). spawn 고정, 흘려쓰기
  exact.py       ExactSolver — RVI/정책평가/greedy. in-RAM 용
  bigexact.py    StreamSolver — 정책반복(PI) + 슬랩 스트리밍 greedy. 대형 전용
  policies.py    벤치마크 B1/B2/B_fast/B_fast_hold/INDEX
  net.py         PolicyNet. carry=False 는 v5b 디코더, True 는 자기회귀
  encode.py      상태 → 패딩 텐서. W 모형이면 슬롯 폭 = W, wcap 동봉
  dcl.py         근사 정책반복 루프
scripts/
  run_one.py / run_sweep.py    기존 정식화 실험
  run_w.py                     W-정식화 정확해 + 벤치마크 + 강제매각 통계
  run_w_dcl.py                 W-정식화 DCL
  measure_loss.py              기존 모형 무료소실 측정
  loss_correct.py              소실보정 (고정정책, 두 경로 교차검증)
  reopt_corrected.py           소실보정 MDP 재최적화 (w3 [1])
  add_bfast_hold.py            B_fast 분해 (w3 [2])
  gap_contamination.py         갭 오염 검정
  make_digest.py               DIGEST 생성
```

## 6. 이 서버에서 일할 때

- **CSR 캐시는 NVMe(`/home/user/batdiag-cache`)로.** `/home/data` 는 회전 디스크라
  53GB CSR 을 정책반복마다 읽으면 대역에 묶인다.
- **GPU 0 에는 Xorg/gnome-shell 이 붙어 있다.** 큰 커널은 워치독에 걸릴 수 있어
  대형 인스턴스는 GPU 1 에서 돌린다. `nvidia-smi` 로 확인할 것.
- **대형 빌드 전 nnz 표본추정 필수.** 표본은 실측 대비 1.5배 정도 과대평가한다
  (W=8: 105.7GB 추정 → 72.1GB 실측).
- 장시간 작업은 nohup/tmux. spawn 워커 정리는 `pkill -f spawn_main`.
  `pkill -f <패턴>` 은 자기 명령줄까지 잡으니 패턴을 분리할 것.
- 두 잡이 같은 GPU 를 쓰면 OOM 난다. 실제로 회귀 테스트가 그렇게 한 번 떨어졌다.

## 7. 읽기 순서

1. `README.md` §4~4.3 — 하드웨어 성능 실측, 스트리밍 경로, 디코더
2. `results/w_model/DIGEST.md` — W-정식화 본 분석 (w2)
3. `results/w_model/DIGEST_w3.md` — 검증 구멍 메우기 (w3)
4. `results/w_model/legacy_loss.md` — 무료소실 오염도와 정오표
5. `src/battery_diag/instance.py` 머리말 — 자원 구조와 가지치기
6. `tests/test_w_model.py`, `tests/test_carry.py` — 사양이 테스트로 고정된 부분

> 원본 HANDOFF 가 지시한 "docs/06 은 §5.2 만 유효" 경고는 `docs/` 자체가 없어
> 옮길 수 없었다.
