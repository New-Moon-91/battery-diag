# BLOCKED — w5 [3] 재산출의 W=7 은 디스크가 모자라 돌리지 못했다

**상태**: 부분 실행. W=4·5·6 은 정확해·벤치마크·정책구조·DCL 까지 전부 냈다.
W=7 만 못 냈다. W=8 은 애초에 규모 밖이다.

## 얼마나 모자라는가

전이배열 저장형식은 int32 인덱스 + float64 확률 = **정확히 12바이트/nnz** 다
(`streambuild._chunk_to_file` 의 assert 로 확인).

| W | 상태수 | nnz 추정 | CSR 추정 | 실측 nnz | 실측 CSR |
|---:|---:|---:|---:|---:|---:|
| 4 | 4,845 | 45,977,435 | 0.6 GB | 39,270,210 | 0.5 GB |
| 5 | 20,349 | 565,973,520 | 6.8 GB | 500,560,760 | 6.0 GB |
| 6 | 74,613 | 6,060,577,716 | 72.7 GB | **5,484,756,410** | **65.8 GB** |
| 7 | 245,157 | 79,203,975,196 | **950.4 GB** | — | — |
| 8 | 735,471 | 290,101,816,678 | 3,481.2 GB | — | — |

추정기는 믿을 만하다 — **w4 의 참값으로 먼저 검산했다.** 같은 표본추정을 w4
설정에 돌리면 W=7 에서 5,183,906,054 가 나오고 실측은 5,156,551,960 으로 **+0.5%** 다.
W=6 에서는 +10.5% 로 조금 높게 잡는다.

관측된 증가율로 외삽해도 결론은 같다: nnz(W6)/nnz(W5) = 11.0, nnz(W5)/nnz(W4) = 12.7
이므로 W=7 은 아무리 낮게 잡아도 **5.0e10 nnz = 600GB 이상**이다.

작업 시점 `/` (NVMe) 여유는 **320GB** 였다.

## 왜 w4 는 같은 W=7 이 됐는데 w5 는 안 되는가

**가지치기 때문이다.** w4 인스턴스는 SM3·쏘울이 모형 기준 R1 이라 `Instance.DUMP` 로
잘려 나갔다 — R1 은 "모든 신호에서 정밀검사가 지배당함"이 증명되는 유형이라
즉시매각 강제가 최적성을 해치지 않는다. w5 인스턴스는 **모형 기준 R1 이 하나도 없어**
네 유형 모두 전 행동을 갖는다.

| | w4 | w5 | 배수 |
|---|---:|---:|---:|
| DUMP | SM3·쏘울 | 없음 | — |
| PUOK | 2종 | 3종 | — |
| (s,a) at W=4 | 65,964 | 201,000 | 3.0배 |
| nnz at W=4 | 9,544,080 | 39,270,210 | 4.1배 |
| nnz at W=7 | 5,156,551,960 | 79,203,975,196 (추정) | 15.4배 |

이것은 모형 결함이 아니라 **인스턴스가 실측 사다리를 반영하도록 바뀐 결과**다.
아이오닉_28.0 은 실측 손익배수 0.63 으로 R1 이지만 모형 내부 기준으로는 R3 이라
가지치기 대상이 아니다. (모형 기준을 쓰는 이유는 `instance.py` 의 REG 주석 참조 —
실측 배수로 가지치면 근거 없이 최적행동을 잘라내게 된다.)

다만 **최적정책은 아이오닉_28.0 을 전량 즉시매각한다** — 가지치기로 강제하지 않아도
스스로 그 결론에 도달한다(`validation.md` §3.3). 즉 손실은 계산량뿐이고 결과는 같다.

## 지우지 않은 것

`/home/user/batdiag-cache` 에 **445GB** 가 들어 있다. 지우면 W=7 공간이 나오지만
그 안에 v5(379GB)·w2~w4 재현용 캐시가 있어 **지우지 않았다.** 기존 결과 재현이
깨지는 것은 되돌릴 수 없고, 이 판단은 사용자 몫이다.

## W=7 이 필요하면

1. **캐시 정리** — 어느 캐시를 버릴지 정해 주면 700GB 를 확보할 수 있다. 가장 큰
   덩어리부터 `du -sh /home/user/batdiag-cache/*` 로 고르면 된다.
2. **`/home/data` 로 이전** — HDD 1.7TB 여유가 있다. `BATDIAG_CACHE=/home/data/batdiag-cache`
   로 돌릴 수 있으나 HDD 라 빌드가 크게 느려진다(W=6 빌드가 NVMe 에서 587초였다).
3. **확률을 float32 로** — 12바이트/nnz 가 8바이트/nnz 로 줄어 950GB → 634GB.
   여전히 모자라고, 정책반복 수렴에 미치는 영향을 따로 검증해야 한다.

셋 중 (1) 이 유일하게 실효적이다. 결정해 주면 이어서 돌린다.

## 결론에 영향이 있는가

없다. W=4·5·6 세 칸에서 확인된 것들 — 한계가치 체감(Δg* 143,642 → 90,860),
선별의 분산(최대 집중 69.6% → 63.8% → 59.9%, **W 가 커질수록 더 퍼진다**),
즉시처분 강제 항의 음수 부호, DCL 1% 이내 — 은 모두 W 에 대해 단조이거나 일관되며
W=7 이 방향을 뒤집을 근거는 없다.
