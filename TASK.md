# w4 — 가격 파라미터 재캘리브레이션 + 차종 재선정 + 재산출

## 먼저: 지금 도는 w3 잔여 작업을 중단하라

`pkill -f spawn_main; pkill -f run_w` 등으로 **W=7·8 학습정책과 차종 사다리 확장을 중단**하라.
이유는 아래 [0] 이다. 이미 커밋된 w3 [1][2][5][6] 결과는 그대로 둔다.

---

## [0] 왜 중단하는가 — 새 실거래 데이터로 가격 파라미터가 틀린 것이 확인됐다

새 자료 2종(재사용 273건 / 재활용 172건, 2022-01~2025-10, 환경공단 거점수거센터 입찰)을
분석한 결과 현행 `data/params.json` 에 두 가지 오류가 있다.

**(a) 낙찰배수의 SOH 계수 `M_s` 가 틀렸다.**
현행 `M_s = -0.9472` (SOH 높을수록 낙찰배수 낮아짐). 실측 재추정하면 부호가 반대이고
**R² = 0.0008 로 설명력이 사실상 0** 이다. 낙찰배수를 실제로 움직이는 것은 입찰참가자
수다(ln 참가자수 계수 +0.284, R²=0.074, 참가자 1명당 약 +9%). 이는 모형 외생이므로
**`M_s = 0` 으로 두고 로그정규 잡음의 평균만 맞추는 것**이 데이터와 정합한다.

**(b) 기간 불일치로 재사용/재활용 배율이 과대평가돼 있다.**
헤도닉 계수는 전기간(2022~2025, 고가 시기 포함)에 가깝고 `p_rc=8,708` 은 2024년 이후
수준에 가깝다. 즉 **재사용은 비싼 시기, 재활용은 싼 시기로 짝지어져** 있다.
그 결과 코나(64kWh, SOH 0.87) 기준 모형 함의 배율이 **8.14배**인데 실측은 **3.20배**다.
현행 파라미터의 재사용 낙찰단가 예측은 2024년 이후 표본에서 **중앙편의 +65.1%,
중앙절대오차 65.2%** 다.

기간을 2024-01 이후로 통일해 두 층을 함께 재추정하면 **중앙편의 +10.7%, 중앙절대오차
28.9%** 로 개선된다. 차종별 편의도 SM3·니로·볼트·봉고3·쏘울·아이오닉·코나 모두
0 수준이 된다.

따라서 g\*·벤치마크 갭·정책 구조 등 가격에 의존하는 모든 수치는 재산출이 필요하다.
(디코더 비교 결론은 가격에 둔감하나, 논문 표에 실을 숫자라면 함께 다시 낸다.)

## [1] 새 파라미터 적용

첨부한 `params_recalibrated.json` 을 `data/params.json` 으로 반영하라.
**기존 파일은 `data/params_v5.json` 으로 보존**하고, 기존 결과 재현이 필요하면
그 경로를 쓰도록 하라. `_calib` 블록에 추정 창·표본수·근거가 들어 있다.

주요 변경: `g_c0` 8.2569→6.1834, `g_cap` 1.5815→1.9936, `g_s` 2.8545→1.8912,
`M_c0` 1.5666→0.7377, **`M_s` -0.9472→0.0**, `M_sd` 0.5455→0.4201,
`p_rc` 8,708→9,491.

`PriceParams.Erev` 는 `M_s=0` 에서도 그대로 동작하므로 코드 변경은 불필요하다.
다만 `M_s` 가 0 이 된 의미(낙찰배수가 SOH 무관)를 코드 주석에 남겨라.

## [2] 차종 재선정 — **SM3 · 쏘울 · 코나 · 볼트 (|T|=4)**

현행 인스턴스(레이·코나·SM3)는 실측 손익배수로 보면 **레이·SM3 가 둘 다 R1** 이고
R2 는 코나 하나뿐이다. w2 에서 "신속검사의 99% 가 코나에 집중" 된 것은 모형 결함이
아니라 **인스턴스에 경계 차종이 없었기 때문**이다.

정밀검사 손익분기 = C_p/q_P = 973,753원. 2024년 이후 실측 손익배수:

| 차종 | 손익배수 | 용량 | SOH중앙 | n(재사용/재활용) |
|---|---:|---:|---:|---|
| SM3 | 0.05 | 26.6 | 67.0 | 10 / 7 |
| 쏘울 | 0.65 | 30.0 | 89.2 | 7 / 6 |
| 볼트 | 1.99 | 60.0 | 96.9 | 19 / 7 |
| 코나 | 2.89 | 64.0 | 86.1 | 27 / 7 |

손익분기 1.0 을 사이에 두고 양쪽에 둘씩 놓이는 촘촘한 사다리다.
넷 다 승용이라 시장이 균질하고 표본도 충분하다.
(포터2가 유일한 R3 후보였으나 상용차이고 2024년 이후 재사용 4건뿐이라 제외.)

`data.py` 의 차종 선택과 `REG` 분류(R1/R2/R3)를 실측 손익배수 기준으로 다시 매겨라.
분류 임계는 손익배수 0.8 / 1.5 를 쓰되, 근거를 코드와 보고서에 남겨라.

## [3] 재산출

W-정식화, |T|=4. 상태공간은 C(W+4|T|, 4|T|) = C(W+16,16) 이므로
**W=6 → 74,613 / W=7 → 245,157 / W=8 → 735,471**. W=7 까지가 현실적이다.
먼저 nnz 를 표본추정해 실제 감당 범위를 확정하고 진행하라 (W=8 은 무리로 예상).

각 W 에서:
- 정확해 g*(W), 한계가치 Δg*/ΔW
- 벤치마크 B1 / B2 / INDEX / B_fast / **B_fast_hold** (w3 [2] 에서 정의한 분해용 중간 벤치마크)
- 최적정책 구조 — **차종별 행동 비율표**. 핵심 질문: 손익배수 사다리를 촘촘히 했을 때
  **선별이 중간 여러 차종에 퍼지는가, 여전히 한 차종에 몰리는가.**
  퍼지면 "선별은 가치 사다리 중간에만 남는다" 주장이 서고, 몰리면 주장을 고쳐 세워야 한다.
- 강제매각 통계
- carry 디코더 DCL, 시드 3개 (legacy 는 결론이 이미 났으므로 생략)

## [4] 실증 검증 절 — 논문의 주요 기여로 갈 부분

**"모형이 예측한 것이 실거래에 나타난다"** 를 보이는 표/그림을 만들어라.

1. **가격 예측 검증**: 새 파라미터로 예측한 재사용 낙찰단가 vs 실거래(2024~ 94건).
   차종별 중앙 실측/예측/편의 표. 현행 파라미터(+65.1%)와 대비해 개선을 보여라.
2. **배율 검증**: 모형 함의 재사용/재활용 배율 vs 실측 배율, 기간별로.
3. **구조 검증**: 실측 손익배수 순서와 모형 최적정책의 검사 강도 순서가 일치하는가.
   이것이 가장 강한 증거다 — 모형이 데이터를 보지 않고 낸 정책 구조가
   독립적으로 계산한 실거래 손익배수와 같은 서열을 내는가.

산출: `results/w4/validation.md`

## [5] 산출물

- `results/w4/summary_w4.csv`, `results/w4/DIGEST_w4.md`, `results/w4/validation.md`
- DIGEST 에는 각 절 끝에 "논문에 들어갈 문장" 한두 줄 명시 (w3 형식 유지)
- 새 결과와 구 결과(v5/w2·w3)의 차이를 명시적으로 대조. 무엇이 바뀌고 무엇이 유지되는가.
- 돌다 만 것·실패한 것 목록
- 회귀 테스트 통과 확인 후 커밋·push

## 진행 원칙

- 우선순위 [1] > [2] > [4] > [3]. **[4] 실증 검증이 [3] 대규모 재산출보다 우선**이다.
  가격 검증은 CPU 로 수 분이면 되고 논문 기여가 크다.
- 대형 빌드 전 nnz 표본추정 필수. 이전 OOM·48시간 무진행 전례를 기억하라.
- 장시간 작업은 nohup/tmux. 막히면 30분 내 BLOCKED.md 후 가능한 부분 진행.
