# w3 — 논문 완성도: 검증 구멍 메우기와 핵심 주장 강화

w2 는 훌륭했다. 특히 [0] 방향 정정과 [3] 갭 오염 검정의 정직성이 좋았다.
이번 단계는 w2 가 남긴 **검증 구멍 네 개**를 메우고, 논문의 핵심 주장 두 개를
받치는 실험을 추가한다. 우선순위 순서대로 적는다. 시간이 모자라면 뒤를 버려라.

---

## [1] 가법분해 재검정을 재최적화로 확정 (필수, 저렴)

w2 [3] 의 보정 g* 는 **기존 최적정책을 고정한 채 보상만 고친 값**이다. 즉 보정
MDP 의 하한이지 최적값이 아니다. 그런데 가법분해 재검정(2.22배→1.25배, 잔차
부호반전)은 이 하한을 최적값처럼 썼다. 논문에 쓰려면 확정이 필요하다.

기존 5칸의 상태공간(1,485~19,305)은 작다. **소실 지점에 강제매각 수익을 넣은
보정 MDP 를 5칸 모두 정확히 다시 풀어라** (기존 NMAX/SMAX 상태공간 그대로,
`min(n+1,NMAX)` 로 잘리는 전이에서 잘린 대수 × p_rc × kWh 를 보상에 가산).

- 재최적화 g* 와 고정정책 보정 g* 의 차이를 보고하라. 차이가 크면 "보정해도
  최적정책 불변" 이라던 w2 의 가정도 깨진 것이다 — 정책 변경률도 함께 내라.
- 재최적화 값으로 가법분해를 다시 검정하라. NMAX/SMAX 효과비와 교호작용 잔차의
  최종 확정치가 이 단계의 산출물이다.
- v5b 철회/유지 목록을 이 확정치로 갱신하라.

## [2] B_fast 의 정의 감사와 갭 분해 (필수, 저렴)

두 가지 문제가 있다.

(a) **임계값이 경계에 붙어 있다.** W=4~8 전부 B_fast_thr=0 이고 B_fast_all 이
    {0,1,2} 세 값뿐이다. 탐색 격자가 좁아서인지 구조적인지 판별하라. 임계의
    정의역을 명시하고(신호구간이 3개면 임계 후보는 몇 개가 전부인가), 격자가
    이미 완전하다면 그렇다고 적으면 된다. 완전하지 않다면 넓혀 다시 찾아라.

(b) **갭 15.76% 의 원인 분해.** w2 (b) 는 갭 확대의 원인을 "최적은 선별보유를
    쓰는데 B_fast 는 못 쓴다" 고 해석했다. 이 해석을 실험으로 확정하라.
    중간 벤치마크 **B_fast_hold** 를 정의하라: 전량 신속검사는 강제하되,
    선별완료품의 보유(HOLD)는 허용 (즉 즉시처분 강제만 해제). 그러면

      gap(B_fast) = [전량검사 강제의 비용] + [즉시처분 강제의 비용]
                  = [gap(B_fast_hold)]    + [gap(B_fast) − gap(B_fast_hold)]

    로 관행의 두 요소가 각각 얼마짜리인지 분해된다. W=4~8 전부에서 계산하라.
    이 분해가 논문에서 "관행의 무엇이 얼마를 잃게 하는가" 절이 된다.

## [3] 경계 인스턴스에서 학습정책 검증 (필수, 비용 추정 후 실행)

"정확해가 닿는 가장 큰 문제에서 학습정책이 그것을 재현한다" 가 논문의 방법론
주장인데, 지금 DCL 은 W=4~6 까지만 돌았다. W=7·8 을 채워라.

- carry 디코더만, 시드 3개. legacy 는 이미 결론이 났으므로 돌리지 마라.
- 먼저 비용을 추정하라: run_dcl 은 라운드당 정확평가를 몇 번 하는가, W=8 평가
  1회가 몇 분인가 (StreamSolver 웜스타트 감안). 총 예상이 8시간을 넘으면
  W=7 3시드 + W=8 1시드로 줄이고 그렇게 보고하라.
- 산출: gap_DCL 평균±SD. W=8 에서 1% 이내면 방법론 주장이 경계까지 닫힌다.

## [4] 차종 사다리 확장 — "선별은 중간에만" 주장의 실질 검증 (핵심 추가 실험)

w2 에서 드러난 약점: 신속검사의 99% 가 코나 한 차종에 집중된다. 차종이 3개뿐이라
"가치 사다리의 중간" 이 사실상 한 점이다. 이 주장을 논문에 세우려면 사다리에
칸이 더 필요하다.

상태공간은 |S| = C(W + 4|T|, 4|T|) 다. 실측 가능 범위:

    |T|=4:  W=6 → 74,613   W=7 → 245,157
    |T|=5:  W=5 → 53,130   W=6 → 230,230

- FLEET 에서 가치 사다리 중간을 채우는 차종을 추가하라. 후보: 쏘울, 아이오닉
  (실측 SOH 가 각 20건·54건으로 충분하고, C_p=50만원 지도에서 중간 가치대다).
  선정 근거를 적어라.
- 권장 구성: **|T|=4 (레이·쏘울·코나·SM3 또는 아이오닉 포함) 에서 W=6·7**,
  여력이 되면 |T|=5 에서 W=5·6. nnz 를 먼저 표본추정하고 W=8 실측(6.0G nnz,
  72GB)과 비교해 감당 범위를 정하라. |T| 가 늘면 도착 조합이 늘어 분기가
  커질 수 있다 — 추정 없이 큰 것부터 걸지 마라.
- 산출: 차종별 행동 비율표 (w2 [2](c) 형식). **질문은 하나다 — 선별이 중간
  여러 차종에 퍼지는가, 여전히 한 차종에 몰리는가.** 퍼지면 주장이 서고,
  몰리면 "선별 대상은 가치와 사전 불확실성의 결합으로 결정된다" 로 주장을
  고쳐 세워야 한다. 어느 쪽이든 결과를 있는 그대로 보고하라.

## [5] φ 민감도 (사전등록 정직성 항목, 저렴)

HANDOFF #4 가 약속한 것이다. FAIL_L(정체불명 9.3%)의 신속검사 탐지가능성
φ ∈ {0, 0.25, 0.5, 0.75, 1} 를 W=6, |T|=3 에서 정확해로 스윕하라.
g*, B_fast 갭, 선별량이 φ 에 얼마나 민감한지가 산출물이다.
결과가 "둔감" 이면 그 자체가 좋은 보고다.

## [6] HANDOFF.md 전면 개정 (필수, 실험 아님)

HANDOFF.md 가 심각하게 낡았다. PPO 시대 서술이 현행처럼 남아 있고, W-정식화·
스트리밍 솔버·자기회귀 디코더·소실 오염 문제를 전혀 모른다. w1~w3 를 반영해
전면 개정하라. 특히:
- §2 "반드시 알아야 할 반전" 에 4번째 반전 추가: NMAX/SMAX → W 정식화 전환과
  그 이유 (무료 소실, 용량 풀링 70%)
- §3 논문급 결과 목록 갱신 (철회된 것 명시)
- §4 열린 작업 표 갱신 (#6 원자료는 해소됐음이 확인됨 — zip 에 실측 CSV 존재)
- §7 읽기 순서 갱신 (docs/06 은 §5.2 만 유효하다는 경고 포함)

## 산출물

- `results/w_model/DIGEST_w3.md` — [1]~[5] 결과와 판정. w2 DIGEST 형식을 따르되
  각 절 끝에 "논문에 들어갈 문장" 을 한두 줄로 명시하라.
- `results/w_model/summary_w.csv` 갱신 (B_fast_hold 열, |T|=4·5 행 추가)
- HANDOFF.md 개정
- 돌다 만 것·실패한 것 목록 (있는 그대로)
- 회귀 테스트 4종 통과 확인, 커밋·push

## 진행 원칙

- 우선순위 [1] > [2] > [6] > [3] > [4] > [5]. [1][2][6] 은 반드시 끝내라.
- 대형 빌드 전 nnz 표본추정 필수. 이전 48시간 무진행·OOM 전례를 기억하라.
- 장시간 작업은 nohup/tmux. spawn 워커 정리는 `pkill -f spawn_main`.
- 막히면 30분 내 BLOCKED.md 작성 후 가능한 부분 진행.
