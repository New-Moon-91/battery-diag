# w6 — W=7 정확해 (야간) + 규모 전이 실험

w5 는 훌륭했다. 특히 메모의 오류 2건(§2.1 기간 불일치, §5 레이 병합 전 값)을 잡아낸 것,
`b_fast_hold` 가 완화가 아니라 별개 고정정책이라 뺄셈 분해가 성립하지 않는다고 정정한 것,
그리고 로그정규 보정 판단(기대보상 최대화이므로 smearing 적용)이 모두 옳다.

이번은 **야간 장시간 작업**이다. 아침에 결과가 남아 있어야 한다.

---

## [0] 최우선 — streambuild 의 병합 피크 문제를 먼저 고쳐라

**이걸 안 고치면 디스크를 비워도 W=7 은 실패한다.**

현행 `streambuild.build_stream` 은 파트 파일을 **전부 만든 뒤** 이어붙이고, 그 다음에 삭제한다:

    for name, ext in (('indices.i32','ix'), ('probs.f64','pr')):
        with open(tmp/name,'wb') as fo:
            for k: copyfileobj(part_k)      # 파트는 아직 살아 있음
    for k: unlink(part_k)                   # 여기서야 삭제

따라서 병합 시점의 디스크 피크 = **파트 전체 + 최종본 전체 = 필요량의 2배**다.
W=7 이 약 660~790GB 라면 피크가 1.3~1.6TB 가 되어 절대 들어가지 않는다.

**수정**: 각 파트를 출력에 복사한 **직후 즉시 unlink** 하라. 피크가
`최종본 + 파트 1개` 로 떨어진다. `indices` 와 `probs` 를 각각 따로 순회하므로,
파트 파일을 (ix, pr) 쌍으로 함께 처리하도록 구조를 바꿔도 좋다.

수정 후 작은 인스턴스로 회귀 테스트(`test_stream_parity`)가 통과하는지 확인하고,
**병합 중 디스크 사용량이 실제로 2배가 되지 않는지 측정**해서 보고하라.

## [1] 디스크 정리 — 단계적으로, 측정하면서

`df -h /home/data` 로 현재 여유를 먼저 기록하라. 그다음 **삭제 후보를 크기순으로 조사**하라
(`du -sh /home/data/batdiag/cache/*` 등).

삭제 우선순위:
1. **구 정식화(NMAX/SMAX) 캐시** — w1 이전 것. 재현이 필요하면 코드로 재생성 가능하고
   현재 논문 결론에 쓰이지 않는다. 먼저 지운다.
2. **w2~w4 의 W-정식화 캐시** — 유형 정의가 `차종`(구) 기준이라 w5 결과와 호환되지 않는다.
   재현 가치가 낮다. 그 다음으로 지운다.
3. **w5 의 W=4,5,6 캐시** — 재생성 비용이 각각 8초 / 79초 / 587초로 **총 11분**에 불과하다.
   공간이 부족하면 이것도 지워라. 나중에 필요하면 다시 만들면 된다.

각 단계마다 `df` 로 확보량을 기록하고 보고하라. **DCL 체크포인트와 results/ 는 절대 지우지 마라.**

## [2] W=7 실행 여부 판정 — 추정 먼저, 그 다음 실행

실측 nnz 성장률은 W4→5 가 12.75배, W5→6 이 10.96배다. 외삽하면
W=7 은 nnz 55~71G, CSR **658~856GB** 다. 폭이 넓어 그냥 시작하면 위험하다.

**반드시 표본추정을 먼저 하라.** 상태를 무작위 표본(예: 200개)해 행동별 분기 수를 세고
전체 nnz 를 추정한 뒤, 12 bytes/nnz 로 CSR 크기를 계산하라.

**판정 규칙** — [0] 의 증분 삭제가 적용된 뒤의 실제 피크는
`CSR 최종본 + 진행 중 파트 1개(≈전체/워커수 ≈ 40GB)` 다. 따라서:

- **추정 CSR + 60GB ≤ 확보 디스크 → 실행**
- 그렇지 않으면 실행하지 말고 [2]를 접어라. BLOCKED_w6.md 에 추정치와 근거를 남겨라.

참고 검산: 확보 최대 765GB 기준, 성장률 10x(CSR 658)면 718 ≤ 765 로 통과,
11x(724)면 784 > 765 로 탈락 경계다. 실측 성장률이 12.75 → 10.96 으로 체감 중이므로
표본추정이 10.5x 이하로 나오면 실행 가능성이 높다.

실행 시:
- `nohup` 또는 `tmux` 로 SSH 단절에 안전하게
- 진행 로그를 남기고, 빌드/해 각 단계 소요를 기록
- 정확해 g*(7), 한계가치 Δg*, 벤치마크 5종, 정책 구조(유형별 행동 비율), 강제매각 통계
- **중간 산출물을 얻는 즉시 커밋·push** 하라. 빌드만 끝나고 해가 안 끝나도 그 시점에 push.

W=6 이 빌드 587초 + 해 1,082초였으니 W=7 은 **수 시간~10시간**을 예상하라.
디스크에서 660GB 를 스트리밍하므로 PI 반복마다 I/O 가 지배할 것이다.

## [3] W=7 이 되면 — DCL 은 돌리지 마라

정확해만으로 충분하다. 학습까지 얹으면 밤을 넘긴다. carry 디코더 갭은 W=6 에서 이미
0.0003% 라 W=7 에서도 결론이 바뀌지 않는다.

## [4] 규모 전이 실험 (W=7 **이전에** 먼저 실행 — 기존 캐시 필요)

**이것이 실규모 확장의 다리다.** 인코더가 순열불변이고 배터리 특징이 6차원 고정이므로,
작은 W 에서 학습한 망을 큰 W 에 그대로 쓸 수 있는 구조다. 실제로 되는지 재라.

- W=4 에서 학습한 carry 정책을 **W=5, W=6 에서 평가**해 정확해 대비 갭을 내라 (시드 3개)
- 같은 규모에서 직접 학습한 정책의 갭과 비교하라
- W=5 → W=6 전이도 같이 재라

**주의**: `ctx` 의 정규화 분모가 Um, Sm 이라 W 에 의존하고, carry 의 잔여용량 신호도
마찬가지다. 규모가 달라지면 망이 못 본 입력 분포에 들어간다. 전이가 실패하면
**정규화를 W 무관 형태로 바꿔 다시 시도**하고, 그 차이를 보고하라.

산출: 전이 갭 표 (학습 W × 평가 W), 정규화 수정 전후 비교.
전이가 되면 "소형에서 검증한 방법을 실규모에 적용한다"는 주장이 근거를 얻는다.
안 되면 "규모별 재학습이 필요하며 비용은 얼마"가 결과다. 어느 쪽이든 논문에 들어간다.

## 산출물

- `results/w6/DIGEST_w6.md` — 각 절 끝에 "논문에 들어갈 문장" 명시 (w5 형식 유지)
- W=7 성공 시 `summary_w6.csv`, `policy_structure_w6.csv`
- 전이 실험 시 `transfer_w6.csv`
- `BLOCKED_w6.md` — 못 한 것과 이유
- 디스크 정리 내역 (무엇을 얼마나 지웠는지) — 나중에 재생성할 때 필요하다
- 회귀 테스트 통과 확인, 커밋·push

## 진행 원칙

- **작업 순서가 중요하다: [0] → [4] → [1] → [2].**
  [4] 전이 실험은 W=4,5,6 의 **기존 캐시 배열이 필요**하다. 캐시를 지우고 W=7 이
  디스크를 점유하면 재생성할 공간이 없어 [4] 를 영영 못 돌린다.
  따라서 **[4] 를 먼저**(기존 캐시로 1~2시간), 끝난 뒤 [1] 정리, 그 다음 [2] 를 야간에 건다.
- **[0] 은 무조건 최우선.** 안 고치면 [2] 가 무의미하다.
- 30분 이상 진전이 없으면 BLOCKED 작성 후 다음으로 넘어가라
- push 가 권한으로 막히면 커밋까지 해두고 명시적으로 알려라
- GPU 두 장을 쓸 수 있으면 병렬 배치하라
