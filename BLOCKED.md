# BLOCKED — w3 [6] HANDOFF.md 개정

**상태**: 실행 불가. 개정할 원본이 존재하지 않는다.

w3 TASK [6] 은 "HANDOFF.md 가 심각하게 낡았다 ... §2/§3/§4/§7 을 개정하라" 고 지시한다.
확인 결과 그 파일은 어디에도 없다.

- `battery-diag/` 작업트리: 없음
- git 이력 전체(`git log --all --name-only`): 등장한 적 없음
- 디스크 전역(`find / -name 'HANDOFF*.md'`): 없음
- `~/battery-diag-v{1,2,3}.zip`: 각 zip 에 `.md` 는 `README.md` 하나뿐
- `docs/` 디렉터리도 같은 이유로 없음 (TASK [6] 의 "docs/06 은 §5.2 만 유효" 도 확인 불가)

같은 이유로 다음도 확인할 수 없다.
- "§2 반드시 알아야 할 반전" 의 기존 3개가 무엇인지 → 4번째를 "추가" 할 수 없다
- "§4 열린 작업 표" 의 기존 항목과 번호 → #6 을 "해소됨" 으로 갱신할 수 없다
- "HANDOFF #4 가 약속한 것" ([5] φ 민감도의 출처) → 다만 [5] 자체는 TASK 본문에
  파라미터가 다 적혀 있어 실행에 지장이 없다

**한 일**: 개정 대신 w1~w3 를 반영한 HANDOFF.md 를 **새로 작성**했다. 없는 문서를
고쳤다고 할 수 없으므로 "신규 작성" 임을 문서 머리에 명시했고, 위 네 항목처럼
원본을 봐야만 쓸 수 있는 내용은 비워 두고 표시했다.

**요청**: 원본 HANDOFF.md 가 다른 곳(로컬 PC, 다른 저장소, 문서도구)에 있으면
붙여 달라. 그러면 지시대로 §2/§3/§4/§7 을 개정하겠다.
