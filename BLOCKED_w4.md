# BLOCKED — w4 [4] 실증 검증의 원자료가 이 머신에 없다

**상태**: 부분 실행. §3(구조 검증)은 완전히 했고, §1(가격)·§2(배율)은 4개 차종만 냈다.

## 무엇이 없는가

TASK [0]·[1] 이 근거로 든 새 자료 2종이 디스크 어디에도 없다.

- `입찰_재사용__0819.xlsx` (재사용 273건, 2024-01 이후 94건)
- `입찰_재활용__0819.xlsx` (재활용 172건, 2024-01 이후 95건)

확인 범위: `~/battery-diag` 작업트리, `git log --all --name-only` 전체 이력,
`~/`·`~/research`·`~/Documents`·`~/Downloads`·`/home/data`, `~/battery-diag-v{1,2,3}.zip`.
`data/` 에 있는 CSV 3종(`pool.csv`, `pass_soh.csv`, `recycle_post.csv`)은 **구 자료**다
— 재사용 381건 / 재활용 652건, 2022.3~2025.3, 날짜 열 없음. 새 자료와 건수·창이 다르다.
`params_recalibrated.json` 의 `_calib` 블록에 파일명만 적혀 있고 파일은 첨부되지 않았다.

## 그래서 무엇을 못 했나

| TASK [4] | 지시 | 실제 산출 | 왜 |
|---|---|---|---|
| §1 | 재사용 낙찰단가 예측 vs 실거래 **94건**, 차종별 중앙 실측/예측/편의 | **4개 차종**(SM3·쏘울·볼트·코나)만 | 실측 중앙값을 TASK [2] 표의 손익배수에서 되풀 수 있는 차종이 그 넷뿐 |
| §1 | 니로·봉고3·아이오닉의 편의 | 없음 | TASK 본문이 "0 수준" 이라고만 하고 값을 주지 않음 |
| §1 | 전체 중앙편의 +65.1%→+10.7%, MdAE 65.2%→28.9% 재현 | 재계산 못 함. TASK 값을 인용만 | 94건이 없음 |
| §2 | 배율 검증 **기간별** | 기간 분해 없음. 단일 시점만 | 거래일자 열이 없음 |
| §3 | 실측 손익배수 서열 vs 최적정책 검사강도 서열 | **완전히 수행** | 손익배수 4종은 TASK 가 줬고 정책은 여기서 품 |

## 되풀기의 근거 (근사가 아님)

손익배수의 정의가 항등식이라 실측 중앙 낙찰가를 정확히 복원할 수 있다.

    r = (E[재사용] - p_rc*kWh) / (C_p/q_P)   ⇒   E[재사용] = r*973,753 + 9,491*kWh

검산: 이 식과 구 파라미터로 코나(64kWh, SOH 0.87)의 모형 함의 배율을 내면 **8.144** 로
TASK [0] 의 "8.14배" 와 소수 둘째 자리까지 맞는다. 정의를 옳게 복원했다는 뜻이다.
남는 오차는 r 이 소수 2자리로 주어진 데서 오는 ±4,869원뿐이다 (SM3 ±1.6%, 코나 ±0.14%).

## 요청

`입찰_재사용__0819.xlsx` / `입찰_재활용__0819.xlsx` 를 `data/` 에 넣어 주면
`scripts/validate_w4.py` 에 로더를 붙여 94건 전수·기간별로 §1·§2 를 다시 낸다.
`_calib` 이 말하는 거래일자·입찰참가자수 열이 있으면 §2 의 기간 분해와
낙찰배수의 참가자수 회귀(R²=0.074)도 재현 가능하다.
